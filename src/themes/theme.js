import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#388e3c",
    },
    secondary: {
      main: "#ffb300",
    },
  },
});

export default theme;
